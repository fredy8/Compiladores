class Dot {
  int x;
  function void setX(int sx) {
    x = sx;
    return;
  }
  function int getX() {
    return x;
  }
}

function void printX(Dot dot) {
  int x;
  x = (dot.getX());
  print(itos(x));
  return;
}

function int main() {
  Dot dot;
  dot.setX(5);
  printX(dot);
}
